/* =================================================================== */
/* ПАЛИТРА ЦВЕТОВ ТЕМЫ                                                 */
/* =================================================================== */

/*
    ОСНОВНЫЕ
    ЦВЕТ ХОЛСТА: #EAE4FF
    ЦВЕТ ТЕКСТА: #6B5B95
    ЦВЕТ РАМКИ: #D6CADD
    ФОН БОКОВОГО МЕНЮ: rgba(244, 240, 255, 230)
    ФОН ЭЛЕМЕНТОВ УПРАВЛЕНИЯ: #FFFFFF

    КНОПКИ (ОБЫЧНЫЕ)
    ЦВЕТ ФОНА: #B5A8E8
    ЦВЕТ ТЕКСТА: white
    ЦВЕТ ФОНА (НАВЕДЕНИЕ): #E0DAFE
    ЦВЕТ ТЕКСТА (НАВЕДЕНИЕ): #6B5B95
    ЦВЕТ ФОНА (НАЖАТИЕ): #D4C9F7
    ЦВЕТ ТЕКСТА (НАЖАТИЕ): #6B5B95

    СПИСКИ И МЕНЮ
    ЦВЕТ ФОНА ВЫПАДАЮЩЕГО СПИСКА: #EAE4FF
    ЦВЕТ ВЫДЕЛЕННОГО ЭЛЕМЕНТА: #ffffff
    ЦВЕТ ЭЛЕМЕНТА (НАВЕДЕНИЕ): #F4F0FF
    ЦВЕТ ВЫБРАННОГО ЭЛЕМЕНТА (ВНУТРИ СПИСКА): #E0DAFE

    ЭЛЕМЕНТЫ УПРАВЛЕНИЯ (COMBOBOX, SPINBOX)
    ЦВЕТ ФОНА КНОПОК: #B5A8E8
    ЦВЕТ ФОНА КНОПОК (НАВЕДЕНИЕ): #A899E0

    ЦВЕТ РАЗДЕЛИТЕЛЯ: #D6CADD
*/

/* =================================================================== */
/* ГЛОБАЛЬНЫЕ СТИЛИ                                                    */
/* =================================================================== */

QWidget {
    background-color: #EAE4FF;
    color: #6B5B95;
    font-size: 14px;
}

/* Кнопки типа "Применить", "Отмена" */
QPushButton {
    background-color: #B5A8E8;
    color: white;
    padding: 10px 20px;
    border: none;
    border-radius: 5px;
}

QPushButton:hover {
    background-color: #E0DAFE;
    color: #6B5B95;
}

QPushButton:pressed {
    background-color: #D4C9F7;
    color: #6B5B95;
}

/* =================================================================== */
/* СТИЛИ ПОЛОСЫ ПРОКРУТКИ                                              */
/* =================================================================== */

/* Вертикальный скролл */
QScrollArea#hotkeysScrollArea QScrollBar:vertical {
    border: none;
    background: transparent;
    width: 8px;
    margin: 0px;
}

/* Стиль ползунка */
QScrollArea#hotkeysScrollArea QScrollBar::handle:vertical {
    background-color: #D4C9F7;
    border-radius: 4px;
    min-height: 25px;
}

/* Стиль ползунка при наведении */
QScrollArea#hotkeysScrollArea QScrollBar::handle:vertical:hover {
    background-color: #E0DAFE;
}

/* Стиль ползунка при нажатии */
QScrollArea#hotkeysScrollArea QScrollBar::handle:vertical:pressed {
    background-color: #B5A8E8;
}

/* Кнопки со стрелками сверху и снизу */
QScrollArea#hotkeysScrollArea QScrollBar::add-line:vertical,
QScrollArea#hotkeysScrollArea QScrollBar::sub-line:vertical {
    height: 0px;
    border: none;
    background: none;
}

/* =================================================================== */
/* СТИЛИ КОНКРЕТНЫХ КОМПОНЕНТОВ                                        */
/* =================================================================== */

/* ----- Холст ----- */

CanvasView {
    border: none;
}

/* ----- Боковая панель инструментов ----- */

/* Боковая панель */
FloatingToolBar {
    background-color: rgba(244, 240, 255, 230);
    border: 3px solid #D6CADD;
    border-radius: 12px;
}

/* Кнопки боковой панели инструментов */
FloatingToolBar QToolButton {
    background-color: transparent;
    border: none;
    padding: 8px;
    margin: 2px;
    border-radius: 6px;
}

FloatingToolBar QToolButton:hover {
    background-color: #E0DAFE;
}

FloatingToolBar QToolButton:pressed {
    background-color: #D4C9F7;
}

/* Разделитель на панели инструментов */
FloatingToolBar QFrame#toolBarSeparator {
    background-color: #D6CADD;
    height: 1px;
    border: none;
    margin: 5px 5px;
}

/* ----- Окно настроек приложения ----- */

/* - Левый список навигации - */
QListWidget#settingsNavigationList {
    border: 1px solid #D6CADD;
    border-radius: 12px;
    outline: 0;
    padding: 3px;
}

QListWidget#settingsNavigationList::item {
    border-radius: 10px;
    margin-bottom: 4px;
    padding: 10px;
}

QListWidget#settingsNavigationList::item:selected {
    background-color: #ffffff;
    color: #6B5B95;
}

QListWidget#settingsNavigationList::item:hover {
    background-color: #F4F0FF;
}

/* - Правый список настроек - */
SettingsDialog  QStackedWidget > QWidget {
    border: 1px solid #D6CADD;
    border-radius: 12px;
    padding: 5px;
}

/* Заголовки на странице горячих клавиш */
QLabel#hotkeySectionHeader {
    color: #6B5B95;
    font-weight: bold;
    font-size: 15px;
    padding-top: 5px;
    padding-bottom: 5px;
    margin-bottom: 5px;
    margin-left: 5px;
    border-bottom: 1px solid #D4C9F7;
}

/* Элементы управления */
SettingsDialog QSpinBox, SettingsDialog QComboBox {
    background-color: #FFFFFF;
    border: 1px solid #D6CADD;
    border-radius: 4px;
    padding: 5px;
}

SettingsDialog QComboBox {
    width: 166px;
}

SettingsDialog QSpinBox {
    min-width: 180px;
}

SettingsDialog QSpinBox:hover, SettingsDialog QComboBox:hover {
    border-color: #B5A8E8;
}

/* Стиль самого выпадающего контейнера */
SettingsDialog QComboBox QAbstractItemView {
    background-color: #EAE4FF;
    border: 1px solid #D4C9F7;
    border-radius: 4px;
    padding: 4px;
}

/* Стиль каждого элемента в списке */
SettingsDialog QComboBox QAbstractItemView::item {
    font-size: 15px;
    min-height: 28px;
    padding-left: 10px;
    background-color: transparent;
    border-radius: 4px;
}

/* Стиль элемента при наведении */
SettingsDialog QComboBox QAbstractItemView::item:hover {
    background-color: #F4F0FF;
}

/* Стиль выбранного элемента */
SettingsDialog QComboBox QAbstractItemView::item:selected {
    background-color: #E0DAFE;
}

/* Область для индикатора-галочки (слева от текста) */
SettingsDialog QComboBox::indicator {
    width: 20px;
    height: 20px;
}

/* Отображаем нашу иконку, только если элемент выбран */
SettingsDialog QComboBox::indicator:checked {
    image: url(:/icons/icons/checkmark-dark.svg);
}

/* Область кнопки со стрелкой у QComboBox */
SettingsDialog QComboBox::drop-down {
    subcontrol-origin: padding;
    subcontrol-position: top right;
    width: 25px;
    background-color: #B5A8E8;
    border-left: 1px solid #D4C9F7;
    border-top-right-radius: 3px;
    border-bottom-right-radius: 3px;
}

SettingsDialog QComboBox::drop-down:hover {
    background-color: #A899E0;
}

/* Сама иконка стрелки */
SettingsDialog QComboBox::down-arrow {
    image: url(:/icons/icons/arrow-down-dark.svg);
}

/* Кнопки вверх/вниз у QSpinBox */
SettingsDialog QSpinBox::up-button,
SettingsDialog QSpinBox::down-button {
    subcontrol-origin: padding;
    width: 25px;
    background-color: #B5A8E8;
    border-left: 1px solid #D4C9F7;
}

SettingsDialog QSpinBox::up-button:hover,
SettingsDialog QSpinBox::down-button:hover {
    background-color: #A899E0;
}

SettingsDialog QSpinBox::up-button {
    subcontrol-position: top right;
    border-top-right-radius: 3px;
}

SettingsDialog QSpinBox::down-button {
    subcontrol-position: bottom right;
    border-bottom-right-radius: 3px;
}

/* Иконки стрелок для QSpinBox */
SettingsDialog QSpinBox::up-arrow {
    image: url(:/icons/icons/arrow-up-dark.svg);
}

SettingsDialog QSpinBox::down-arrow {
    image: url(:/icons/icons/arrow-down-dark.svg);
}
